{
  "name": "tribunal",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "A panel of four advocates and three judges deliberates a charge sheet and returns three reasoned verdicts, never combined.",
  "scripts": {
    "build": "rm -rf dist && mkdir -p dist && cp -R public/. dist/ && cp test/fixtures/t001.json dist/t001.json && node scripts/build-prompts.mjs && tsc -p tsconfig.browser.json",
    "typecheck": "tsc --noEmit",
    "test": "node --test \"test/**/*.test.ts\""
  },
  "devDependencies": {
    "@netlify/functions": "^2.8.2",
    "@types/node": "^26.4.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=22"
  }
}
